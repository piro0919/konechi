# 仕様

壁打ちで決まったことを置く場所。決まっていないものは「保留」に残す。

## 決まったこと

### 名前

**Konechi（コネち）。** コネクション由来。アプリ名とキャラ名を同じにする。

選ぶ物差しは「有線にも無線にも寄らないこと」。このアプリは両方を出し分けるのが役目なので、
片方に寄った語源だともう片方が名前から漏れる。

- 同名のソフトは見つからなかった。`konechi.com` はアジア雑貨の通販で分野が違う
- `github.com/piro0919/konechi` と `konechi.kkweb.io` はどちらも空いていることを確認済み
- 見送った候補と理由
  - Mikochi — 「みこち」がさくらみこの愛称として通っており、オリジナルキャラの独自性がぼやける。同名の OSS もある
  - Denpachi — 語源が電波なので有線側が漏れる
  - Tsunachi — Twitch に同名の配信者、App Store に語感の近い Tsunagi がある
  - Musubichi — おむすびを連想させる

### 表示するもの

- メニューバーには**アイコンだけ**を出す。文字は添えない
- 出すのは**実際に通信に使われている主経路ひとつ**だけ。有線と Wi-Fi が同時につながっていても、裏で生きている側はメニューバーには出さない

### アイコンの方向性

**カラーのキャラクターを置き、状態は表情で表す。**
パワプロの調子アイコンのように、有線が絶好調、Wi-Fi が好調、未接続が絶不調。

先に「持ち物やポーズで輪郭そのものを変える」案を採り、頭の上にプラグ・弧・バツを置く形まで詰めたが、
最終的に表情差分へ切り替えた。判断の経緯を残す。

- chappie-desktop の状態別アイコンを実寸（17pt）で確かめた結果、**表情の差はまったく読めなかった**。
  `idle` と `listening` は並べて比べても区別が付かない。読めたのは赤い禁止マークの付いた `muted` だけ
- つまり実寸での判別を担えるのは、表情ではなく形と色である
- それでも表情差分を採ったのは、**状態がしょっちゅう切り替わるものではなく、
  分からなければメニューを開けば書いてあるから**。実寸での絶対的な判別より、絵の気持ちよさを優先した
- 頭の上に印を足す案は、試作の過程で次の問題が出た。口にケーブルをくわえさせると気持ち悪くなり、
  頭の横に小さく乗せると読めず、バツ印は「利用者が何か失敗した」ように見えた

**この判断の代償**: メニューバーのアイコンだけでは有線か Wi-Fi かが分からない。
確定させるにはメニューを開くことになる。

アイコンの高さは **22pt**。メニューバーの厚みがちょうど22ptで、これが上限。
設定で記号（SF Symbols）に切り替えることもできる。

### キャラクター

- **人型のちびキャラ。かわいい女の子の顔。** chappie-desktop と同じ絵柄の路線
- **髪型は太く短めのツインテール。** 左右に張り出すぶん輪郭が特徴的になり、実寸で残る情報が増える。
  細く長いと毛先が消えるうえ、横幅を食って顔が縮む。枠は正方形なので、横に広げたぶん顔が小さくなる
- **髪はピンク系。** 実寸で残るのは輪郭と色の面積だけなので、髪の色が実質的な識別子になる
  - 青系は通信の連想と合うが、メニューバーは暗い背景のことが多く濃い青は沈む
  - 白や淡色は chappie の `sayo` が実寸でも明るい帯で消えていなかったが、それは縁取りがあるからで、縁取りの設計に成否が寄る
  - ピンクは背景の明暗を選ばず彩度で存在感が出るので、17pt では一番安全

### 絵の条件（実寸22ptで成立させるため）

- 背景は透過。正方形に近く、余白は詰める
- 顔の占有率を高く取る。細部で個性を出さない。実寸で最初に潰れるのは耳・尻尾・髪飾りのような細部
- 輪郭に縁取りを入れる。メニューバーは壁紙が透けるので、明るい壁紙でも暗い壁紙でも沈まないようにする
- 色数を絞り、線は太めにする
- 状態は表情だけで表す。輪郭を変えたり、持ち物を足したりしない
- **4枚は共通の枠で切り詰め、同じ大きさに描かれていること。** 1枚でも縮尺が違うと、
  状態が切り替わったときにキャラの大きさが跳ねる
- **絵の縮小は `size` の指定で行い、その大きさの画像へ描き直さない。**
  描き直すと1倍の画像になり、Retina で引き伸ばされて粗くなる

### VPN のとき

- **下の物理経路のキャラに、右下へ鍵のバッジを重ねる**。
  状態そのものは表情で表すが、VPN は滅多に無い状態なので、ここだけ印を使う
- 有線か Wi-Fi かの情報を保ったまま、VPN であることを足せる
- バッジは実寸で読めるよう色を付ける。chappie の `muted` の赤い禁止マークが、実寸で唯一はっきり読めた前例
- 記号一枚で VPN を表す案（`network.badge.shield.half.filled` など）は採らない。実寸で潰れて判別できない

### 判定の仕方（実装で裏を取った内容）

- 主経路が VPN のトンネルで有線でも Wi-Fi でもないときは、`Setup:/Network/Global/IPv4` の
  `ServiceOrder` を優先順に見て、`State:/Network/Service/<id>/IPv4` から使用中のインターフェースを引き、
  最初に見つかった有線か Wi-Fi を下の物理経路として採る
- リンク速度は `ifconfig` の `media:` 行を読む。ioctl の `SIOCGIFMEDIA` は定数が Swift へ取り込めない。
  メニューを開いたときにしか呼ばないので、外部コマンドの費用は問題にならない。
  Wi-Fi は `autoselect` としか出ないため、速度なしとして扱う
- 通信量は `getifaddrs` の `if_data` から累計バイト数を読み、差分を取る。`netstat -ibn` と一致を確認済み
- メニューバーの常駐は SwiftUI の `MenuBarExtra` ではなく AppKit で組む。
  メニューの中身は OS 側のメニューとして描かれるため SwiftUI からは開閉の通知が取れず、
  「通信量はメニューを開いている間だけ測る」が実現できない

### 判定の仕方

- 構成データベース（`State:/Network/Global/IPv4`）の `PrimaryInterface` を読み、その種別が Ethernet か IEEE80211 かで決める
- アイコンの見た目や `networksetup` の並び順からは推測しない。`route get default` と同じ経路を見る

### メニューを開いたときに出すもの

- 接続の種類・サービス名・デバイス名・IP アドレス・ルーター・リンク速度・下り・上りの8項目
- **下りと上りは別の項目に分ける。** 1行にまとめると数字が2つ並んで読み取りにくい
- 通信量だけだと、開いた瞬間にたまたま何も流れていない場合に「遅い」のか「使っていない」のか区別できない。リンク速度と並べて初めて切り分けになる
- メニューバー本体と違い、メニューの中は開いた人が見たくて開いた場所なので、項目が多いことの害は小さい。迷ったら載せる
- 通信量だけは固定値ではないので、**メニューを開いている間だけ**読み直す。閉じている間は測らない

### 設定

**設定画面を作る。** メニューの中にチェック項目を並べる形は採らない。

置く項目は6つ。残り2つは前提が揃ってから足す。

| 項目                     | 状態   | 待っているもの   |
| ------------------------ | ------ | ---------------- |
| ログイン時に起動する     | 実装済 | -                |
| 通信量の単位             | 実装済 | -                |
| Konechi について（版数） | 実装済 | -                |
| メニューに出す項目の取捨 | 実装済 | -                |
| アイコン（コネち／記号） | 実装済 | -                |
| 言語（日本語・英語）     | 実装済 | -                |
| 更新を確認               | 実装済 | -                |

- **言語の既定は英語。環境が日本語のときだけ日本語にする。** 設定で明示的に選ぶこともできる
  （システムに従う／日本語／English）。判定には `Locale.preferredLanguages` の先頭を使う
- 表示文字列は `.lproj` を使わず Swift の表（`Sources/Localization.swift`）に置く。
  ビルドを自前の shell で組んでいて、文字列だけのために資源の仕組みを足すと `build.sh` が重くなる。
  言語が2つしかないうちはこの形のほうが追いやすい
- 言語を変えたら**設定画面ごと作り直す**。文字列は組み立て時に焼き込まれるため、
  開いたままの画面に後から反映できない

- **メニューに出す項目の取捨は、隠したものだけを保存する。** 出すものを保存すると、
  後から行が増えたときに既存の利用者へその行が出なくなる
- **接続の行だけは隠せない。** このアプリの本体なので、隠す意味がない

- ログイン時の起動は `ServiceManagement` に登録する。状態は OS 側が持つので自前では覚えず、
  開くたびに問い合わせる。二重に持つと必ずずれる
- ad-hoc 署名のまま、`/Applications` の外（`~/Repository/konechi`）からでも登録できることを確認済み。
  登録後にアプリを起動し直しても有効のままだった

### クリックしたときの動き

**左クリックでメニューを開くだけ。** それ以外は持たせない。
右クリックの別動作や、クリックでキャラが反応する演出は入れない。

### 配布

アプリ本体はネイティブの Swift のまま作り、配布の形を既存の Mac アプリ（galopen）に合わせる。

- GitHub Releases に DMG を置く
- 署名は **ad-hoc**。Developer ID の署名も公証もしない
  - 無署名だと「壊れているため開けません」になり利用者に打つ手が無くなる
  - ad-hoc なら「未確認の開発元」警告になり、システム設定の画面から許可できる
- 自動更新を入れる。初回の許可以降、**利用者が端末を触る場面を作らない**
  - galopen は Tauri の更新機能を使っている。ネイティブでは Sparkle が定番で、考え方は同じ
  - ad-hoc 署名のまま Sparkle の自動更新が通るかは未確認。作る前に調べる
- ランディングページを Next.js で用意し、`konechi.kkweb.io` に置く

### 自動更新

galopen と同じ挙動に揃える（`galopen/src-tauri/src/lib.rs` の `check_for_updates` を確認した結果）。

- 確認は**起動時に1回だけ**。定期的なチェックはしない
- 見つかったら**毎回ダイアログで確認**し、了承されたら取得して入れ替える。「以降は黙って入れる」という記憶はしない
- 仕組みは **Sparkle 2.9.5**。framework は大きいのでリポジトリに置かず、`build.sh` が `Vendor/` へ取ってくる
- 起動時は `checkForUpdateInformation()` で黙って確かめ、**見つかったときだけ**画面を出す。
  `checkForUpdates()` を直に呼ぶと、最新のときも「最新です」の画面が出てしまう
- `SUEnableAutomaticChecks` と `SUAutomaticallyUpdate` を false にする。
  既定のままだと初回起動で「自動で確認していいか」を尋ねる画面が出て、galopen の挙動とずれる
- 更新の署名鍵は生成済み。公開鍵は `build.sh` の Info.plist に入っている。
  **対になる秘密鍵はログインキーチェーンにあり、失うと更新を配れなくなる**
- 更新情報の置き場は `https://github.com/piro0919/konechi/releases/latest/download/appcast.xml`。
  リポジトリはまだ作っていないので、今は確認しても何も見つからず黙って終わる
- 性質として、常駐アプリは何日も起動しっぱなしになるため、再起動するまで更新は届かない。
  galopen も同じ性質を持っており、揃えることを優先した

## 必要な絵

標準は確定済み（`Resources/konechi-default.png`）。残りは標準を元にした表情差分。

| 絵                     | 表情           | 状態   |
| ---------------------- | -------------- | ------ |
| `konechi-default.png`  | 素の顔。穏やかな笑み。アプリ本体のアイコンや LP にも使う | 配置済 |
| `konechi-wired.png`    | 絶好調         | 配置済 |
| `konechi-wifi.png`     | 好調・ウインク | 配置済 |
| `konechi-offline.png`  | 絶不調・眠そう | 配置済 |
| `konechi-lock.png`     | VPN の鍵バッジ | 配置済 |

- キャラの4枚は**共通の枠で余白を切り詰める**（`./Tools/trim.py` に4枚まとめて渡す）。
  1枚ずつ詰めると表情ごとに描かれた範囲が違うぶん大きさが変わり、状態が切り替わるたびに跳ねる
- 生成側は透過のつもりで**市松模様を描いてくることがある**。`hasAlpha` だけでは見抜けないので、
  四隅の画素の不透明度も確かめる

## アプリ本体のアイコン

macOS 26 は、**透過を含まない正方形の絵**を受け取ると自分で角丸に切り抜く。
少しでも透過が入っていると「角丸を描けていない絵」とみなし、システムが薄い板を敷いて
その上に載せる。角丸の中にもう一枚角丸が入った見た目になるのはこれが原因。

`NSWorkspace.icon(forFile:)` の結果を並べて実測した。

| アプリ  | 不透明な画素 | 見え方       |
| ------- | ------------ | ------------ |
| Galopen | 100%         | 角丸。板なし |
| Mekuri  | 40%          | 板の上に載る |

したがって**角丸を自分で描かず、背景色で端まで塗った正方形**を渡す。
組み立ては `./Tools/make-icon.py <角丸つきの元絵.png>`。

- 生成に頼んだ角丸つきの絵は、背景色を採るためだけに使う
- 角丸の曲線を Apple と同じ超楕円に合わせても板は消えない。**判定しているのは透過の有無**
- **アイコンはシステムに強く覚え込まれる。** 同じ場所のアプリを差し替えても古いまま返ってくる。
  確かめるときは別の場所へ複製してから見る

## 保留

いまのところ無し。

## 検討に使う道具

確認用の入口がアプリ本体にもある。実際にその状態を作らずに見た目を確かめられる。

```bash
./Konechi.app/Contents/MacOS/Konechi --state wired|wifi|offline|other
./Konechi.app/Contents/MacOS/Konechi --vpn        # 鍵バッジを出す
./Konechi.app/Contents/MacOS/Konechi --settings   # 設定画面を開いた状態で起動
```

```bash
./probe.sh     # 判定ロジックの結果を端末で確かめる
./mockup.sh    # 記号の見本を docs/ に作り直して開く
./preview.sh <png>…   # 絵をメニューバーの実寸で並べて確かめる
```
